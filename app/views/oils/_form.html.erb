<%= form_for @oil do |f| %>

  <% if @oil.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@oil.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @oil.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :photo_url %><br>
    <%= f.text_field  :photo_url %>
  </p>

  <p>
    <%= f.label :benefits %><br>
    <%= f.text_area :benefits %>
  </p>

  <p>
    <%= f.submit %>
  </p>

 <% end %>

 <%= link_to 'Back', oils_path %>
